export class Task {
    constructor(id, title, priority, completed, isDeleted){
        this.id = id,
        this.title = title,
        this.priority = priority,
        this.completed = completed,
        this.isDeleted = isDeleted
    }
}
